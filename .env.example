# @BotFather dan olingan token — Deno Deploy: Project → Settings → Environment Variables → BOT_TOKEN
# Qiymatda qo'shtirnoq, < > yoki bo'sh qator bo'lmasin
BOT_TOKEN=

# HTTP port (lokal; Deploy odatda avtomatik)
PORT=8000
